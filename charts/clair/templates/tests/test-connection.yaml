apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "clair.fullname" . }}-test-connection"
  labels:
{{ include "clair.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "clair.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
